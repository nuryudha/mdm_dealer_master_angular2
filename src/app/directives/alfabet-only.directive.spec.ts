import { AlfabetOnlyDirective } from './alfabet-only.directive';

describe('AlfabetOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AlfabetOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
